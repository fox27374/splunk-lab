all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
  children:
    sh:
    # Searchhead
      hosts:
        spl-sh2.ntslab.loc:

    master:
    # Deployment, Monitoring, Licensing
      hosts:
        spl-master.ntslab.loc:

    cm:
    # Clustermaster
      hosts:
        spl-cm.ntslab.loc:

    idx:
    # Indexer
      hosts:
